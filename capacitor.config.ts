import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.collectionmemories.starter',
  appName: 'Collection Memories',
  webDir: 'public',
  bundledWebRuntime: false
};

export default config;
